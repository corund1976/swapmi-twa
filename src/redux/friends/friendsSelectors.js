const list = (state) => state.friends.list

const ticketsReward = (state) => state.friends.ticketsReward
const tokensReward = (state) => state.friends.tokensReward

export default {
  list,

  ticketsReward,
  tokensReward,
}